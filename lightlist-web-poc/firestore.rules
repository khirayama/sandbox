rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development: Allow all operations for testing
    // In production, replace with appropriate security rules
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
